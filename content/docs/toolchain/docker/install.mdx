---
title: "如何安装 Docker"
---

<DraftNotice />

## 我们的目标

<Callout type="success">
**成功安装 Docker 并运行一个测试容器**
</Callout>

## 开始之前

由于 Docker 限制，Windows 和 macOS 系统需要额外安装 Docker Desktop，它不够轻便，因此本教程将以 Linux 系统为例进行演示。

Windows 系统请使用 WSL2 进行安装，macOS 系统请使用 Colima。

- [ ] Linux 发行版（如 Ubuntu、Debian、Fedora 等）
- [ ] 具有 sudo 权限的用户账户

## 动手操作

### 1. 添加官方源 

```
sudo apt update
sudo apt install ca-certificates curl gnupg

sudo install -m 0755 -d /etc/apt/keyrings

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo \
"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu \
$(. /etc/os-release && echo $VERSION_CODENAME) stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

### 2. 安装 Docker Engine

```
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

### 3. 开机自启

```
sudo systemctl start docker
sudo systemctl enable docker
sudo systemctl status docker
```

### 4. 权限

```
sudo usermod -aG docker $USER
newgrp docker
```

### 5. 网络代理

```
sudo nano /etc/docker/daemon.json
```

```
{
  "proxies": {
    "http-proxy": "http://192.168.1.7:7897",
    "https-proxy": "http://192.168.1.7:7897",
    "no-proxy": "localhost,127.0.0.1"
  }
}
```

### 6. 测试

```
docker run hello-world
```