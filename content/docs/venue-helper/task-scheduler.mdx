---
title: "如何预约抢票"
---

任务计划程序能够定时执行 Python 脚本。

## 动手操作

### 1. 创建任务（task）

建议勾选「使用最高权限运行」，以避免权限不足可能导致的运行失败。

### 2. 配置触发器（trigger）

新建，定时，`12:29:55`

### 3. 配置操作（action）

选中「启动程序」，程序/脚本选择虚拟环境中的 `python`，添加参数选择 `main.py` 的绝对路径

开始于 `main.py` 的绝对路径所在的目录